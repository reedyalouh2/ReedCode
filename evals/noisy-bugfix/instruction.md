The pricing tests are failing.

Before modifying anything, run ./run_tests.sh.

Inspect the implementation and tests, identify the root cause, make the smallest correct fix, then run ./run_tests.sh again and verify it passes.
