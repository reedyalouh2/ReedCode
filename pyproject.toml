[project]
name = "reedcode"
version = "0.4.0"
description = "A minimal instrumented coding-agent harness for studying agent workloads"
requires-python = ">=3.12"
dependencies = [
    "harbor==0.23.0",
    "httpx==0.28.1",
    "openai==2.54.0",
]

[tool.uv]
package = false
